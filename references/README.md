# References

This directory is for local reference clones and inspiration material that should not be committed.

The current reference project is:

```sh
gh repo clone delhoume/trmnl_wordclock references/trmnl_wordclock
```

`references/*` is ignored by git, except for this README.
